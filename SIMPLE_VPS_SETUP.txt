
╔═══════════════════════════════════════════════════════════════╗
║  🎯 SIMPLE VPS BACKEND SETUP - 10 MINUTES                     ║
╔═══════════════════════════════════════════════════════════════╗

📌 YOUR ARCHITECTURE:
────────────────────────────────────────────────────────────────
Netlify (Frontend) → Your VPS (Backend) → Neon + Cloudinary


🚀 PART 1: SETUP VPS BACKEND (5 minutes)
────────────────────────────────────────────────────────────────

1. SSH into your VPS:
   ssh root@YOUR_VPS_IP

2. Install Node.js:
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt install -y nodejs

3. Install PM2:
   sudo npm install -g pm2

4. Create backend folder:
   mkdir -p ~/portfolio-backend
   cd ~/portfolio-backend

5. Upload your backend files (choose one):
   
   Option A - Git:
   git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git .
   
   Option B - SCP (from your computer):
   scp -r server package.json package-lock.json root@YOUR_VPS_IP:~/portfolio-backend/

6. Create .env file:
   nano .env
   
   Paste this:
   CLOUDINARY_CLOUD_NAME=dodqmzv8c
   CLOUDINARY_API_KEY=177678439452387
   CLOUDINARY_API_SECRET=Y8T6e-oV4n1ImUpKQB8bfvuJJcU
   DATABASE_URL=postgresql://neondb_owner:npg_8MX4cUdeBqAH@ep-plain-wind-aesquc2e.c-2.us-east-2.aws.neon.tech/neondb?channel_binding=require&sslmode=require
   PORT=3005
   NODE_ENV=production
   
   Save: Ctrl+X, Y, Enter

7. Install dependencies:
   npm install --production

8. Start backend:
   pm2 start server/index.js --name portfolio-api
   pm2 save
   pm2 startup
   (Run the command it outputs)

9. Open firewall:
   sudo ufw allow 22
   sudo ufw allow 3005
   sudo ufw enable

10. Test it works:
   curl http://localhost:3005/api/health
   
   Should return: {"status":"ok", ...}


💻 PART 2: UPDATE FRONTEND (3 minutes)
────────────────────────────────────────────────────────────────

1. Edit src/lib/api.ts:
   
   Change line 4 from:
   const API_BASE_URL = import.meta.env.VITE_API_URL || "http://YOUR_VPS_IP:3005";
   
   To:
   const API_BASE_URL = "http://YOUR_ACTUAL_VPS_IP:3005";
   
   Example:
   const API_BASE_URL = "http://45.123.45.67:3005";

2. Rebuild frontend:
   npm run build

3. Deploy to Netlify:
   - Go to: https://app.netlify.com
   - Find site: dedouleur
   - Drag dist/ folder to deploy area
   - Wait 1 minute


🧪 PART 3: TEST EVERYTHING (2 minutes)
────────────────────────────────────────────────────────────────

1. Open: https://dedouleur.netlify.app
   Should load your portfolio

2. Open admin: https://dedouleur.netlify.app/x9k2m8p7q4n6
   Password: KJr5iCm9iwsMsl50ojXSHpTe

3. Upload an image/video
   Should upload to Cloudinary

4. Save projects
   Should save to Neon database

5. Refresh page
   Projects should still be there!


✅ YOU'RE DONE!
────────────────────────────────────────────────────────────────

Your setup:
✓ Frontend: Netlify (free, fast)
✓ Backend: Your VPS (always on!)
✓ Database: Neon (free, permanent)
✓ Storage: Cloudinary (25GB free)

No sleep, no data loss, full control! 🎉


🔄 UPDATING YOUR BACKEND
────────────────────────────────────────────────────────────────

SSH to VPS:
   ssh root@YOUR_VPS_IP
   cd ~/portfolio-backend
   git pull origin main
   npm install --production
   pm2 restart portfolio-api


📊 USEFUL COMMANDS
────────────────────────────────────────────────────────────────

Check backend status:
   pm2 status
   pm2 logs portfolio-api

Restart backend:
   pm2 restart portfolio-api

Test API:
   curl http://localhost:3005/api/projects
   curl http://localhost:3005/api/health


🆘 TROUBLESHOOTING
────────────────────────────────────────────────────────────────

CORS errors:
   - Check VPS IP in src/lib/api.ts matches actual IP
   - Rebuild frontend: npm run build
   - Redeploy to Netlify

Backend not responding:
   pm2 logs portfolio-api
   pm2 restart portfolio-api

Can't connect to VPS:
   - Check firewall: sudo ufw status
   - Test: curl http://YOUR_VPS_IP:3005/api/health


═══════════════════════════════════════════════════════════════
           🎉 ENJOY YOUR ALWAYS-ON PORTFOLIO! 🎉
═══════════════════════════════════════════════════════════════

